import 'package:flutter/material.dart';

// TODO Colores principales de la app
const kPrimaryColor = Color.fromARGB(255, 202, 193, 184);
const kSecondaryColor = Color.fromARGB(255, 85, 61, 40);
const kAmbertColor = Colors.amber;

//Colores de alertas
const colorError = Colors.red;
const colorsWhite = Colors.white;
const colorSucces = Color(0xFF00E676);
